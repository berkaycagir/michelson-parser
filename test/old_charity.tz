parameter address;
storage string;
code {UNPAIR; 
      CONTRACT unit;
      IF_SOME {} { FAILWITH; }; 
      PUSH mutez 1000000;
      UNIT;
      TRANSFER_TOKENS;
      NIL operation; SWAP; CONS; PAIR};
